<div class="mt-4">
  <div class="row">

    {{# each courses}}
    <div class="col-sm-6 col-lg-4" style="padding-top: 75px;">
      <div class="card card-course-item">
        <a href="/courses/{{this.slug}}">
          <img class="card-img-top" src="{{this.img}}" alt="{{this.name}}" style="border-radius: 10px;">
        </a>
        <div class="card-body">
          <a href="/courses/{{this.slug}}">
            <h5 class="card-title">{{this.name}}</h5>
          </a>
          <a href="/courses/{{this.slug}}" class="btn btn-primary" style="border-radius: 10px;">Xem nội dung</a>
        </div>
      </div>
    </div>
    {{/each}}
  </div>
  <a href="/courses/create" class="btn btn-primary" style="border-radius: 50%; position: fixed; top: 600px; right: 50px;height: 60px; width: 60px; "><span class="oi oi-plus" style="padding-top: 37%; padding-left: 2px"></span></a>
</div>